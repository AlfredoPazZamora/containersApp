export interface LoginInterface {
    email: String;
    password: String;
}

export interface ContainersInterface {
    id: String;
    origin: String;
    destiny: String;
}

export interface WorkerInterface {
    id: String;
    name: String;
    surname: String;
    ocuppation: String;
}